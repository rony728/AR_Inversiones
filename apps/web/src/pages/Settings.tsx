import { Link } from 'react-router-dom';
import { ShieldCheck, Tags, Truck, UserRound, UsersRound } from 'lucide-react';
const items = [[UserRound, 'Usuarios', 'Acceso de los operadores', '/configuracion/usuarios'], [UsersRound, 'Socios', 'Rony, Alex y Brian con dos fondos', '/configuracion/socios'], [Tags, 'Categorías', 'Organización del catálogo', '/configuracion/categorias'], [Truck, 'Proveedores', 'Datos de compras', '/configuracion/proveedores']] as const;
export function Settings() { return <><section className="page-title"><div><p className="eyebrow">ADMINISTRACIÓN</p><h2>Configuración</h2><p>Catálogos y parámetros necesarios para la operación.</p></div></section><section className="settings-list">{items.map(([Icon, title, text, path]) => <Link key={title} to={path}><span><Icon size={21} /></span><div><h3>{title}</h3><p>{text}</p></div><ShieldCheck size={18} /></Link>)}</section></> }
