import { Link } from 'react-router-dom';
import { Database, ShieldCheck, Tags, Truck, UserRound, UsersRound } from 'lucide-react';
const items = [[UserRound, 'Usuarios', 'Acceso de los operadores'], [UsersRound, 'Socios', 'Rony, Alex y Brian con dos custodias'], [Tags, 'Categorías', 'Organización del catálogo'], [Truck, 'Proveedores', 'Datos de compras'], [Database, 'Sincronización', 'Dispositivos y operaciones pendientes']] as const;
export function Settings() { return <><section className="page-title"><div><p className="eyebrow">ADMINISTRACIÓN</p><h2>Configuración</h2><p>Catálogos y parámetros necesarios para la operación.</p></div></section><section className="settings-list">{items.map(([Icon, title, text]) => <Link key={String(title)} to={title === 'Socios' ? '/custodias' : title === 'Categorías' ? '/productos' : title === 'Proveedores' ? '/compras' : '/configuracion'}><span><Icon size={21} /></span><div><h3>{title}</h3><p>{text}</p></div><ShieldCheck size={18} /></Link>)}</section></> }
